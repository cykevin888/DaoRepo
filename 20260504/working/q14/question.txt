Question
The current selected programming language is Java 21. We emphasize the submission of a
fully working code over partially correct but efficient code. Once submitted, you cannot
review this problem again. You can use system.out.println to debug your code. The
system.out.println() may not work in case of syntax/runtime error. The version of Java
21 being used is 21.0.4. An alternate sort of a list consists of alternate elements
(starting from the first position) of the given list after sorting it in an ascending
order. You are given a list of unsorted elements. Write an algorithm to find the
alternate sort of the given list.

Input
The first line of the input consists of an integer size, representing the size of the
given list (N). The second line consists of N space-separated integers arro, arr1,
elements of input list.

Output
arry, representing the Print space-separated integers representing the alternately
sorted elements of the given list.

Constraints
0< size≤ 106 -106 s arr≤ 106 0 <i< size

Example

Input:
8
351591026
Output: 1359

Explanation:
After sorting, the list is [1, 2, 3, 5, 5, 6, 9, 10]. So, the alternate elements of the
sorted list are [1, 3, 5, 9].

----------------------------------------
中文翻译
----------------------------------------

题目描述：
先将给定列表按升序排序，然后从排序后的结果中取出下标为 0、2、4、...
的元素，形成“交替排序”结果。

输入：
第一行是列表长度 N。
第二行是 N 个整数。

输出：
输出交替排序后的元素序列。
